spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://10.10.10.105:9999
      fail-fast: true
      password: SpingCloudConfiguration
      username: user

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0